<Alloy>
	<Collection src="characters"></Collection>
    <Window class="container">
        <TableView dataCollection="characters">
            <TableViewRow title="{characterName}" characterName="{characterName}" image="{image}" onClick="showCharacter"></TableViewRow>
        </TableView>
    </Window>
</Alloy>